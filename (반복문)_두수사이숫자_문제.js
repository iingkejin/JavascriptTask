/*** 두 수 사이 숫자 구하기 ***/

function answer(x, y) {
  let result = [];
  // 코드 구현 
  return result;
}


let input = [

];
for (let i = 0; i < input.length; i++) {
  process.stdout.write(`#${i + 1} `);
  console.log(answer(input[i][0], input[i][1]));
}
